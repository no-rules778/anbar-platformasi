# Phase 11 parity ledger — Module L («İdarə paneli»)

> **PHASE 11 ACCEPTED — 2026-09-11 (authoritative).** Independent Codex
> review found no further application defect. Final gate: focused Phase 11 +
> App 9 files / 189 tests, full suite 151 files / 3270 tests, typecheck,
> oxlint, sandbox build and `git diff --check` clean; staging empty. The
> 55-row tally remains 28 `CODE VERIFIED`, 26 `LIVE VERIFIED`, 1 `IN
> PROGRESS` (M11-91 admin/rehber live-comparison boundary), 0 `NOT STARTED`,
> 0 `BLOCKED`, 0 unclassified. The retained boundary is not promoted and does
> not block the approved read-only scope.
> [Final audit](../audits/2026-09-11-phase11-final-codex-acceptance.md).

> **IMPLEMENTED + LIVE SWEEP — 2026-09-11 (latest). Next step: Codex's final
> independent audit.** The owner accepted the complete recommended package
> (D-L1…D-L5) and Codex's design audit passed, so T1-T4 were executed as one
> block. **Tally, measured mechanically from the 55 `| M11-* |` rows: 28
> `CODE VERIFIED`, 26 `LIVE VERIFIED`, 1 `IN PROGRESS` (M11-91, the admin
> comparison leg), 0 `NOT STARTED`, 0 `BLOCKED`, 0 unclassified; 55 unique
> ids, 0 duplicates.** Gate: full suite 151 files / 3270 tests, `tsc -b
> --noEmit`, `oxlint src`, sandbox build, `git diff --check` all clean, 0
> staged. One read-only TEST browser sweep as the anbardar: 0 production
> hits, 0 write attempts. Phase 11 remains **NOT ACCEPTED**.
> [Evidence](../audits/2026-09-11-phase11-implementation-live-check.md) ·
> [Proposal](./2026-09-11-react-migration-phase11-dashboard-proposal.md) ·
> [Plan](../plans/2026-09-11-react-migration-phase11-dashboard.md) ·
> [Design audit](../audits/2026-09-11-phase11-design-codex-audit.md) ·
> [Owner decision](../decisions/2026-09-11-phase11-design-scope.md).

> **DESIGN DRAFT — 2026-09-11 (HISTORY; superseded by the banner above).**
> At design acceptance the ledger held 55 unique `M11-*` rows, all
> `NOT STARTED`, with no application code. That tally is history; the current
> figure is at the top of this file.
> [Handoff audit](../audits/2026-09-11-phase11-design-handoff.md).

Evidence classes: *unit/source* = vitest / static source comparison;
*browser* = interception on the sandbox dev server against TEST; *live* = the
row's exact contract exercised against TEST `alkjjbaawmsirsfvqljm`; nothing in
this phase is persisted-write evidence. Legacy refs are `index.html` lines.

Rows deliberately NOT in this ledger (prose boundary, see proposal §2.10 and
§6): `Tam ixrac` / `Excel (SON formatı)` (D-L2, Phase 16), the rail counter
badges (shell, Phase 18), and `Çap` (the dashboard has no print button).

| Row | Contract | Legacy ref | Status |
|---|---|---|---|
| M11-01 | Rail contains one «İdarə paneli» entry as the FIRST entry of the «Əməliyyat» group, above «Yeni əməliyyat»; clicking it opens the page and it becomes the only active entry | 250-251, 1505-1506 | `LIVE VERIFIED` — live: exactly one «İdarə paneli» entry, FIRST in the «Əməliyyat» group above «Yeni əməliyyat», `a.on` = that entry only; App tests: position, single active entry, present for admin/rehber/anbardar |
| M11-02 | The entry and page are ungated for admin, rehber and anbardar: no id, no `display:none`, no `go()` branch | 251, 1495-1503 | `CODE VERIFIED` — App tests: the entry renders and opens for admin, rehber and anbardar; the nav source test proves no `isAdmin` guard on the entry. Live: anbardar only (admin/rehber identities unavailable, M11-91) |
| M11-03 | After sign-in the default page is `dash` for every role (subject to D-L1; the current interim `refs`/`log` default is replaced) | 7523, 3009, `App.tsx:111` | `LIVE VERIFIED` — live: sign-in landed on the dashboard with `a.on` = «İdarə paneli»; App tests: admin AND non-admin default to `dash`; nav test pins `page ?? 'dash'` and the absence of the old `refs`/`log` branch |
| M11-04 | Heading «İdarə paneli»; subtitle is `(w or 'Bütün anbarlar') · son əməliyyat tarixi {last} · nf(movs.length) hərəkət qeydi`, where `last` is the newest operational date over ALL warehouses (raw ISO) or `—`, and `w` prints RAW (no `whLabel`) | 278, 1548-1550 | `LIVE VERIFIED` — live «Bütün anbarlar · son əməliyyat tarixi 2026-09-03 · 3 hərəkət qeydi»; with the foreign anbar selected the count fell to 0 while the date stayed 2026-09-03 and the RAW name printed; lib + page tests incl. the `—` case |
| M11-05 | Warehouse selector offers «Bütün anbarlar» (value `''`) plus every active `anbar` name, text through `whLabel()`, value raw; changing it re-derives KPIs, donut, both tables and the subtitle | 281, 1535-1540, 933 | `LIVE VERIFIED` — live options `['', 'Test Anbar', 'CODEX Phase8 Transfer Anbar']` with raw values; selecting one re-derived KPIs/donut/tables/subtitle with ZERO new requests; page test pins the `whLabel` text vs raw value split |
| M11-06 | The selected warehouse survives navigation away and back (store-held, not component-local) | long-lived DOM; M4-18 / M9-06 rule | `LIVE VERIFIED` — live: «CODEX Phase8 Transfer Anbar» selected, navigated to «Mal hərəkəti» and back, selection still «CODEX Phase8 Transfer Anbar»; store test proves the module-level state |
| M11-07 | Selector option set follows D-L4: legacy builds it once (1536) and never rebuilds; the approved deviation rebuilds from the current snapshot and keeps a still-valid selection, else resets to `''` | 1536 | `CODE VERIFIED` — store tests: a still-valid selection survives a reload; a warehouse no longer active resets to `''`; «Bütün anbarlar» is never reset. TEST offered no warehouse-deactivation event to exercise live |
| M11-10 | Snapshot reads exactly `movements`, `items`, `warehouses`, `partners` (partners only for the VÖEN rule); no `stock_conditions`, `stock_layers`, valuation table or RPC. If D-L3 defers the alerts card, the set is exactly the Phase 10 three reads | 867-871, 1174, 7003 | `LIVE VERIFIED` — live: `GET movements, items, warehouses, partners` ×2 (StrictMode) and nothing else; no `stock_conditions`, `stock_layers`, valuation table or snapshot RPC from this page; api test: four readers once each, raw rows through |
| M11-11 | The snapshot is one atomic generation: any read error or rejection yields `ok:false` and nothing is applied (accepted Phase 8-10 deviation from legacy partial rendering) | 855, 1017; M10-11 precedent | `CODE VERIFIED` — api tests: any reader's returned error OR rejection yields `ok:false` with no snapshot, with per-reader fallback messages; store test: a first failure leaves nothing applied |
| M11-12 | First load shows a loading block; a first-load failure shows «Yükləmə xətası» and no dashboard; a failed REFRESH retains the previous complete snapshot (rows, indexes, selector) and shows the «Yenilənmədi» tag; recovery clears it | M10-12 precedent | `LIVE VERIFIED` — live: 8 injected 503s gave «Yenilənmədi injected 503» with 5 KPIs / 1 top row / 3 recent rows retained, no load-error block, then clean recovery; a second window of network aborts gave «Yenilənmədi TypeError: Failed to fetch» with the same retention and recovery; store + page tests |
| M11-13 | Realtime watches exactly `movements`, `items`, `partners`, `warehouses` through the shared hook with its 400 ms debounce; a burst produces one reload | 1163-1181 | `CODE VERIFIED` — page test: `useRealtimeRefresh(true, ['movements','items','partners','warehouses'], fn)` with no debounce override (the hook's 400 ms default, pinned by its own M9-131 tests); the callback re-loads the store. No realtime event occurred live |
| M11-14 | A successful realtime-triggered reload toasts «Məlumatlar yeniləndi (digər istifadəçi)»; a manual/first load and a failed reload do not | 1169; `ReferenceDirectoryPage.tsx:62` precedent | `CODE VERIFIED` — page test: a successful realtime-triggered reload shows «Məlumatlar yeniləndi (digər istifadəçi)»; the mount load and a FAILED reload show nothing (negative control). No second writer existed to fire it live |
| M11-15 | A response from an earlier load that arrives after a later load's response is discarded (success and failure alike) | M10-15 precedent | `CODE VERIFIED` — store tests: an older success and an older failure that settle after a newer reply are both discarded, leaving the newer snapshot and `loading:false` |
| M11-16 | Operational movements come from the shared `excludeCancelled()` (`normalMovements()`); no dashboard code re-derives cancellation | 1249-1270 | `CODE VERIFIED` — store test: an unbalanced cancelled pair leaves only the operational row (`operational = ['c']`, `q = 3`) through the shared `buildItemIndexes()` → `excludeCancelled()` |
| M11-17 | Balances come from the unchanged shared `buildItemIndexes()` (`IX.bal`: `q = +(in−out).toFixed(4)`, price from the ITEM, `val = q × price`); `itemIndex.ts` and `operationalMovements.ts` show no diff | 1271-1320 | `CODE VERIFIED` — the store calls `buildItemIndexes(items, movements)` unchanged; `lib/itemIndex.ts` and `lib/operationalMovements.ts` show no diff. Live cross-check: dashboard «Qalıq dəyəri» 80.00 ₼ / 1 mövqe equals the Phase 9 balances page's «Ümumi dəyər» 80.00 ₼ / «Mövqe sayı» 1 on the same data |
| M11-18 | No client-side warehouse narrowing: the selector list is the unscoped active-`anbar` list and the data is whatever RLS returns | D2 / M8-42, M9-17/18, D-K3 | `CODE VERIFIED` — api test: no user and no warehouse argument in any read. Live (anbardar): the selector listed BOTH anbars unscoped while the data was RLS-shaped, the foreign anbar rendering `—` / `0 mövqe` / 0 rows. Admin half unavailable (M11-91) |
| M11-20 | Scope derivation: `bal` = balances of the selected warehouse or all; `movs` = operational rows of the selected warehouse or all; `pos` = `bal` with `abs(q) > 1e-9` (exactly `1e-9` excluded, `1.1e-9` and `−0.5` included) | 1541-1543 | `CODE VERIFIED` — lib tests: `w=''` returns the same arrays by identity; a selected warehouse filters both; `pos` excludes exactly `1e-9` and `0` while keeping `1.1e-9` and `−0.5` |
| M11-21 | KPI 1 «Qalıq dəyəri»: `money(Σ bal.val)` (a zero total renders `—`), sub `nf(pos.length) aktiv mövqe`, class `g` | 1551-1552 | `LIVE VERIFIED` — live «Qalıq dəyəri» `80.00 ₼` / «1 aktiv mövqe» with class `kpi g`; the foreign anbar rendered `—` / «0 aktiv mövqe»; lib test pins `money(0) = —` with the count kept |
| M11-22 | KPI 2 «Ümumi mədaxil»: `nf(Σ in, 2)`, sub «bütün dövr üzrə», no class | 1545, 1553 | `LIVE VERIFIED` — live «Ümumi mədaxil» `11.00` / «bütün dövr üzrə» with class `kpi` and no modifier; lib + page tests |
| M11-23 | KPI 3 «Ümumi məxaric»: `nf(Σ out, 2)`; sub is `0% dövriyyə` when Σin = 0 (literal `0`, not `0.0`), else `(out/in×100).toFixed(1)% dövriyyə` with a dot decimal; class `o` | 1545, 1554 | `LIVE VERIFIED` — live `3.00` with «27.3% dövriyyə» (3/11, dot decimal) on the full set, and the exact literal «0% dövriyyə» on the zero-inbound foreign anbar; lib tests pin `0%` ≠ `0.0%` and the 66.7% rounding |
| M11-24 | KPI 4 «Satınalma məbləği»: `money(Σ over type Satınalma of in × (movement price or item price or 0))` — movement price FIRST; only a falsy movement price (including `0`/`null`) falls back to item price, while a negative movement price is truthy and is preserved exactly as legacy does; sub «qiyməti bəlli sətirlər üzrə»; class `v` | 1546, 1555 | `CODE VERIFIED` — lib tests: the movement price wins; `0`/`null` fall back to the item price and an unknown item to 0; a non-Satınalma type is ignored; a NEGATIVE movement price is truthy and is preserved (`-6,00 ₼`, not `20,00 ₼`). Live rendered `—` because the TEST purchases carry type «Alış», not «Satınalma», so this row's branches are not live-exercised |
| M11-25 | KPI 5 «Qiyməti olmayan mövqe»: `nf(count of pos with falsy price)`; sub «dəyərləndirmə natamamdır» and class `r` when > 0, else «hamısı qiymətlidir» and class `g` | 1547, 1556 | `LIVE VERIFIED` — live `0` / «hamısı qiymətlidir» / class `kpi g` (every TEST position is priced); lib + page tests drive the `r` branch and prove zero-quantity rows are excluded |
| M11-26 | KPI markup and order: five `.kpi{ cls}` blocks in the order above, each `.eyebrow` label, `.v` value, `.s` sub | 1551-1556 | `LIVE VERIFIED` — live: five `.kpi` blocks in the legacy order with classes `kpi g, kpi, kpi o, kpi v, kpi g`, each carrying `.eyebrow` / `.v` / `.s`; page test pins labels, values, subs and classes |
| M11-30 | Bar chart data: one entry per active `anbar` in configured order, INDEPENDENT of the selector; `v = Math.round(Σ IX.bal[w].val)`; `sub = nf(count abs(q) > 1e-9) mövqe`; sorted by `v` descending | 1558-1561 | `LIVE VERIFIED` — live bars for both active anbars (`Test Anbar 80.00 ₼ · 1 mövqe`, `CODEX Phase8 Transfer Anbar — · 0 mövqe`), value-descending, and UNCHANGED while the foreign anbar was selected (the selector-independence control); lib tests pin `Math.round`, the epsilon count and the sort |
| M11-31 | Bar chart rendering: `max = Math.max(1, …v)`; label with `title` attribute; value `money(v)`; sub right-aligned; `<div class="bar"><i style="width:{(v/max×100).toFixed(1)}%;background:var(--steel)">`; a zero-value row has width `0.0%` | 1368-1381, 1562 | `LIVE VERIFIED` — live: a `title` attribute per label, the `money` value, a right-aligned sub, and `.bar > i` widths `100%` / `0%` (CSSOM-normalised) over a `.bar` computed height of `6px`; the legacy `toFixed(1)` string itself is pinned on `barWidth()` (`100.0%`, `33.3%`, `0.0%`) with `barMax` never below 1 |
| M11-32 | Hint under the bar chart: «Dəyər = qalıq × son məlum vahid qiyməti. Qiyməti daxil edilməmiş mallar sıfır dəyərlə iştirak edir.» | 1563 | `LIVE VERIFIED` — the exact hint «Dəyər = qalıq × son məlum vahid qiyməti. Qiyməti daxil edilməmiş mallar sıfır dəyərlə iştirak edir.» observed under the bar chart |
| M11-33 | Donut data: distinct movement types of the selector-scoped `movs`, value = count, colours `Satınalma #0E7C6B`, `Əvvələ qalıq #1F4E6B`, `Yerdəyişmə #5B4B9E`, `Silinmə #A9231C`, `Sahəyə #B06A11`, `Qaytarma #71838F`, `İcarə #7A5C29`, any other type `#71838F`; sorted by count descending | 1565-1567 | `LIVE VERIFIED` — live donut `Alış: 2` (fallback `#71838F`, a type absent from the legacy palette) and `Silinmə: 1` (`#A9231C`), count-descending, and it FOLLOWED the selector (empty on the foreign anbar); lib test pins the full colour map and the `Satış` fallback |
| M11-34 | Donut geometry and legend: `viewBox 0 0 148 148`, `R 62`, `r 38`, centre `74,74`, start `−π/2`, large-arc flag when the angle exceeds π, one `<path>` per type with `<title>{type}: {nf(count)}</title>`, legend rows with 9 px swatch, type name and bold `nf(count)`; the single-type degenerate arc is reproduced, not fixed | 1382-1395 | `CODE VERIFIED` — chart test: the path strings equal the legacy formula recomputed in the test (positive control) and differ from a wrong start angle (negative control); large-arc flag 1 above π and 0 below; `viewBox 0 0 148 148`; `<title>` texts; legend swatch/name/count. Live: 2 paths with the exact legacy titles and legend rows `Alış2`, `Silinmə1` |
| M11-35 | Chart empty states: bar chart with no warehouses renders an empty grid; donut with no movements renders an SVG with no paths and an empty legend (`tot` falls back to 1, no division by zero) | 1370, 1383 | `LIVE VERIFIED` — live: selecting the movement-less foreign anbar rendered an SVG with 0 paths and an empty legend, no error; chart tests cover the empty bar grid and the `tot → 1` fallback |
| M11-40 | «Dəyərə görə ilk 10 mövqe» rows are `pos` sorted by `val` descending, first 10 (the 11th is excluded; fewer than 10 renders all) | 1570 | `CODE VERIFIED` — lib test: 11 positions yield the top 10 by value with the 11th excluded, fewer than 10 renders all, and the input is not mutated. Live: TEST holds one position, so the cap is not live-exercised |
| M11-41 | Top-10 table: columns «Mal · Anbar · Qalıq(r) · Dəyər(r)»; cells name-div + code-span, `whLabel(w)`, `nf(q,2) unit`, `money(val)`; rows `clk`; zero rows render the `tbl()` empty block | 1571-1574, 1409-1414 | `LIVE VERIFIED` — live headers `Mal · Anbar · Qalıq(r) · Dəyər(r)`, row class `clk`, cells «TEST Mal 1» + code `0000001`, `Test Anbar`, `8.00 ədəd`, `80.00 ₼`; the legacy empty block observed on the foreign anbar; page test |
| M11-42 | «Son əməliyyatlar» rows are `movs` sorted by `created_at` DESC then date DESC (a missing/invalid `created_at` sorts as 0), first 10 | 1576 | `LIVE VERIFIED` — live order 03.09 → 02.09 → 01.09 by `created_at` DESC; lib test proves `created_at` beats date, that a null/invalid timestamp sorts as 0 and falls to date order, and the 10-row cap |
| M11-43 | Recent table: columns «Tarix · Mal · Növ · Miqdar(r)»; cells `fmtD(date)` + recorder hint (`recorderLabel`), item name with code fallback (`or`, so an empty name falls to the code) + `whLabel(w)` code-span, type tag class via `typeTagClass`, `+nf(in,2)` in `var(--in)` when in > 0 and `−nf(out,2)` (U+2212) in `var(--out)` when out > 0, both when both | 1577-1583, 1415-1418, 990 | `LIVE VERIFIED` — live: `03.09.2026` with the recorder hint `anbar-admin-test@example.com` (the directory branch), item name + `Test Anbar` code-span, tag classes `t-mut`/`t-rm`, `+1.00` in `var(--in)` and `−3.00` (U+2212) in `var(--out)`; page tests cover all four recorder branches and the both-signs row |
| M11-44 | «Hamısı» in the recent-table header navigates to «Mal hərəkəti» and moves the active rail entry | 289, 1363 | `LIVE VERIFIED` — live: «Hamısı» opened «Mal hərəkəti» and moved `a.on` to that entry; page test asserts the callback and the App test the page switch |
| M11-45 | Clicking a row in either table opens the existing `ItemCard` for that code with the Phase 10 handoffs (operation prefill → `op`, edit → `nom`), and closing it returns to the dashboard | 1856-1858, M10-48 | `LIVE VERIFIED` — live: a top-10 row opened «Mal kartoçkası · 0000001» with «Bu mal üzrə əməliyyat», closing returned to the dashboard, and a recent row opened the same card; page test with the REAL `ItemCard` reaches `onOpenOperation`/`onEditItem`; App test proves the store handoffs |
| M11-50 | Alerts card «Diqqət tələb edən məsələlər»: with no groups renders the hint «Avtomatik yoxlamalar problem aşkarlamadı.»; otherwise a `.pill-row` of `btn` pills (`dgr` added when `sev === 'high'`) reading `{title} — {nf(count)}` | 1585-1588 | `LIVE VERIFIED` — live: no rule fired on TEST, so the exact hint «Avtomatik yoxlamalar problem aşkarlamadı.» rendered; page tests drive the populated branch (pill order, `dgr` on high, «{title} — {count}») and the selector-independence control |
| M11-51 | `controlIssues()` is one pure function returning groups in the fixed order neg, nop, doc, voen, orph, tr, lag, dup, fut, wo, each with `id`, `sev`, `title`, `why`, `cols`, `rows`, `codes`; a rule with no rows is omitted; it is GLOBAL (not selector-scoped) | 6996-7031 | `CODE VERIFIED` — lib tests: clean data yields no group; a mixed fixture yields exactly `neg, nop, doc, dup, fut` in that order with matching severities/titles and a complete group shape (`why`, `cols`, `rows`, `codes`, row width = column count) |
| M11-52 | Rule `neg` (high, «Mənfi qalıq»): balances with `q < −1e-9`; exactly `−1e-9` excluded | 6998 | `CODE VERIFIED` — lib test: `q = −1` and `−1.1e-9` are counted while exactly `−1e-9` and `0` are excluded; the row prints the ALIASED warehouse (`Xocahəsən` → `Xocəsən`) |
| M11-53 | Rule `nop` (med, «Qiyməti olmayan qalıq»): non-zero positions with falsy price; a zero-quantity priceless row is NOT counted | 7000 | `CODE VERIFIED` — lib test: a non-zero priceless position is counted; a priced one and a zero-quantity priceless one are not |
| M11-54 | Rule `doc` (low, «Sənədsiz satınalma»): operational `Satınalma` rows with empty invoice AND empty contract; either present excludes | 7002 | `CODE VERIFIED` — lib tests: neither invoice nor contract is flagged; either one present is not; a non-Satınalma type is ignored; an empty partner prints `—` |
| M11-55 | Rule `voen` (low, «VÖEN-siz kontragentdən alış»): `Satınalma` with a non-empty partner not in the internal set (every location name ∪ active warehouses ∪ the six literals) and with no partner record of that exact name carrying a VÖEN | 7003-7004 | `CODE VERIFIED` — lib tests: an external partner with a blank VÖEN and an unknown partner are flagged; a partner with VÖEN, an empty partner, and every internal counterparty (location name, warehouse name, each of the six literals) are not; the name match is case- and whitespace-exact |
| M11-56 | Rule `orph` (high, «Nomenklaturada olmayan mal»): operational rows whose code is absent from the item catalogue | 7006 | `CODE VERIFIED` — lib test: a code absent from the catalogue is flagged with the net quantity (`in − out`); a known code is not |
| M11-57 | Rule `tr` (high, «Cütü olmayan yerdəyişmə»): transfer rows with no pair, where the counterparty is the FIRST configured warehouse that is a prefix of the partner text, and a pair is an opposite-direction transfer of the same code in that warehouse with quantity difference `< 1e-6` (same date preferred); a partner text matching no warehouse is unpaired | 7009-7016 | `CODE VERIFIED` — lib tests: a matched opposite-direction transfer pairs; no counterpart, and a partner text prefixed by no configured warehouse, are unpaired; a quantity difference of exactly `1e-6` is unpaired and `0.9e-6` pairs; reordering `DB.whs` flips the FIRST-prefix choice and with it the outcome |
| M11-58 | Rule `lag` (med, «Yerdəyişmənin tarixləri uyğun gəlmir»): outbound transfers whose pair exists and whose absolute day difference is `> 3` (exactly 3 excluded; a missing date counts as 0) | 7017-7018 | `CODE VERIFIED` — lib tests: a pair 4 days apart is flagged (`Fərq (gün)` = 4), exactly 3 days is not, and a missing date counts as 0 days |
| M11-59 | Rule `dup` (med, «Nomenklaturada təkrar»): groups of items whose names collapse to the same key after lower-casing and removing whitespace and `/ . , " ' -`; count is the number of GROUPS | 7019-7022 | `CODE VERIFIED` — lib test: three items collapsing to one normalised key produce ONE group row listing all three codes and their balances; a distinct name produces none |
| M11-60 | Rule `fut` (med, «Gələcək tarixli qeyd»): operational rows dated after `today()` (UTC ISO); today itself is NOT future | 7023-7024 | `CODE VERIFIED` — lib test: `today + 1` is flagged with the recorder label; `today` itself is not |
| M11-61 | Rule `wo` (high, «İri məbləğli silinmə»): `Silinmə` rows with `out × ITEM price > 500` (movement price ignored; exactly 500 excluded) | 7025-7026 | `CODE VERIFIED` — lib tests: `out 51 × item price 10 = 510` is flagged and exactly `500` is not; a movement price of 1000 on a zero-price item is NOT flagged, so the movement price is ignored |
| M11-62 | Pill activation follows D-L3: legacy navigates to `ctrl`; until Phase 15 the pill is inert and carries a `title` naming the unmigrated screen, or the card is deferred entirely | 1587, 1363 | `CODE VERIFIED` — D-L3 as approved: page test proves every pill carries `title` = «Nəzarət və risklər hələ köhnə platformadadır», `aria-disabled="true"` and an inert click. Live: TEST produced no group, so only the empty branch rendered |
| M11-70 | All figures and dates use the shared `nf`, `money`, `fmtD`, `whLabel` (az-AZ separators, `money(0) = —`); the sole exception is the KPI 3 `toFixed(1)` percentage pinned by M11-23 | 592-602 | `LIVE VERIFIED` — live `80.00 ₼`, `8.00 ədəd`, `11.00`, `03.09.2026`, `—` for a zero value, and the single `27.3%` dot-decimal exception, all through the shared helpers; `lib/format.ts` and `lib/movementRoute.ts` show no diff |
| M11-71 | Layout: KPI row, then a `1.35fr 1fr` grid (bar card, donut card), then a `1fr 1fr` grid (top-10 card, recent card with the «Hamısı» header button), then the full-width alerts card; 12 px vertical gaps | 282-291 | `CODE VERIFIED` — source: `.kpis`, then a `1.35fr 1fr` grid (bar card, donut card), then a `1fr 1fr` grid (top-10 card, recent card with the «Hamısı» header button), then the full-width alerts card, each `marginTop: 12`. Live: all five blocks rendered in that order; exact pixel geometry is a Phase 18 visual-review matter |
| M11-72 | Stylesheet rules the page depends on (`.eyebrow`, `.grid`, `.kpis`, `.kpi`, `.kpi.g/.o/.r/.v`, `.kpi .v`, `.kpi .s`, `.bar`, `.bar i`, `.pill-row`, `.chart`) exist per D-L5, ported verbatim | CSS 30, 66-72, 89-90, 121, 151 | `LIVE VERIFIED` — `index.css.dashboard.test.ts` compares all sixteen declaration blocks against index.html 30, 66-72, 89-91, 121 and 151 verbatim and checks every token they reference is defined on `:root`; live confirmation that the port is effective: `.bar` computed height `6px`, where the class had no rule at all before this phase |
| M11-73 | Recent-table item name falls back to the code with `or` semantics (an empty-string name shows the code), never `??` | 1581 | `CODE VERIFIED` — page test: an item whose catalogue name is empty renders the CODE, proving legacy `or` semantics rather than `??` (every live row has a name) |
| M11-74 | Top-10 item name is the index name, so an orphan code shows «(nomenklaturada yoxdur: CODE)» | 1311, 1573 | `CODE VERIFIED` — page test: an orphan balance renders «(nomenklaturada yoxdur: ZZZ)» from the index name (TEST has no orphan position) |
| M11-90 | All TEST evidence is read-only: no mutation RPC or table write, no production contact, no fixture, no layer change, no cutover | safety boundary | `LIVE VERIFIED` — the whole sweep ran under a blanket intercept: **0 production hits**, **0 write attempts**; the only non-GET traffic was the session/RPC allowlist (`register_session`, `get_user_directory`, `stock_layers_supported`, `list_my_sessions`, `end_session`, `HEAD audit_log`); no fixture, no layer change, no cutover; signed out through the real dialog |
| M11-91 | Anbardar and admin renderings are compared live without added client narrowing; if only the anbardar identity is available the admin leg is recorded as an evidence boundary, not promoted | D-K3 / M10-51 precedent | `IN PROGRESS` — **anbardar half live**: an unscoped warehouse LIST (both anbars in the selector and both bars) with RLS-shaped DATA (only Test Anbar carries positions/movements; the foreign anbar renders `—` / `0 mövqe` / empty tables), and the API sends no narrowing (test). **Admin half open**: no TEST admin (or rehber) identity was available in this session |

Rows may be promoted only from exact implementation/live evidence, and only
after Codex accepts this design and the owner settles D-L1…D-L5. Phase 11 is
**NOT STARTED / NOT ACCEPTED**.
