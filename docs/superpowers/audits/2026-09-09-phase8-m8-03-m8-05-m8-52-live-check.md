# Phase 8 — M8-03 / M8-05 / M8-52 read-only live check

Date: 2026-09-09 (Asia/Baku)

Target: TEST Supabase `alkjjbaawmsirsfvqljm`. Production `bbjmhaerssakbreykxiw`
was never contacted (**0 attempts**).

Read-only throughout: no mutation RPC, no fixture, no application-code change,
no commit/stage/push/deploy, no I-10 row. `Çap` is outside acceptance.

## Environment

- `web/.env.sandbox.local` → TEST URL, `VITE_ALLOW_LOCAL_WRITES=false`,
  `VITE_TEST_ENVIRONMENT=true`, verified before browser use.
- Localhost freshly started as
  `npm run dev -- --mode sandbox --host 127.0.0.1 --port 5175` (PID 18976,
  `sandbox` badge); HTTP 200.
- Playwright 1.63.0 driving installed Chrome from the npm `_npx` cache; not
  added to `web/package.json` or `web/node_modules`.
- Real React login as TEST admin; session ended through the real «Çıxış»
  (`rpc/end_session` observed).

## Three separately named datasets

Kept distinct throughout, as required:

| Dataset | Definition | Size |
| --- | --- | --- |
| **RAW** | the snapshot `movements` response body | **101 rows** |
| **OPERATIONAL** | independently recomputed surviving set | **3 rows** |
| **RENDERED** | rows actually in the DOM `<tbody>` | **3 rows** |

Valuations read: **3 rows** from `writeoff_valuations`.

The snapshot request was matched by its **full column signature**
(`contract_num` + `created_by` + `doc_num` + `created_at`), so the one narrower
`movements` request issued by another screen could not be mistaken for it. That
narrower request was counted (1) and excluded.

---

## M8-52 — widened explicit column list: **LIVE VERIFIED**

Ledger contract: `movements` column-list widening for `contract_num` and the
recorder; `api/itemMovements.api.ts` selects `contract_num` and `created_by`.

The «Mal hərəkəti» snapshot reuses `fetchItemMovements()` (via
`movementsSnapshot.api.ts`), so this is verified on the exact request the page
issues, not an incidental one.

**Decoded select list, in request order — 16 columns:**

```
id, item_code, warehouse, date, in_qty, out_qty, price, partner, type,
invoice_num, note, doc_num, created_at, channel, contract_num, created_by
```

| Proof | Observed |
| --- | --- |
| both widened fields requested | `contract_num` YES · `created_by` YES |
| required fields missing | **none** (0 of 16) |
| extra fields beyond the contract | **none** |
| column-explicit, not `select=*` | YES |
| returned row keys cover the required set | YES (16 keys, exact match) |
| deterministic order before range | `order=date.asc,created_at.asc` (A11) |
| paging legs | one page, `offset=0&limit=1000` — a single short page ends the loop |
| snapshot-signature requests seen | 2 (**one logical load**; React StrictMode duplicates it — 1 distinct URL) |
| other narrower `movements` requests | 1, correctly excluded from this evidence |

Fields consumed by M8-03/M8-04/M8-05 (`note`, `doc_num`, `id`, `type`,
`out_qty`, `price`, `created_by`, `contract_num`) are all sourced from **this**
response.

**Not claimed:** payload size or large-volume behaviour — M8-52 does not
require it (that is M8-53), and it was not measured here.

---

## M8-03 — row source is `excludeCancelled()`: **LIVE VERIFIED**

The surviving set was **reimplemented independently** in the harness from the
documented legacy rules (`index.html:1249-1270`). The application's
`excludeCancelled()` was **not imported, called or reused**.

### Cancellation families present in the live fixture — all three

| Family | Marker shape | Occurrences |
| --- | --- | --- |
| whole-document reversal | `Ləğv: <doc>` / `Ləğv (əks yerdəyişmə): <doc>` | **35** |
| legacy row-level | `Ləğv ID: <id>` | **12** |
| legacy transfer pair | `Ləğv (əks yerdəyişmə) ID: <a>:<b>` | **2** |

`hiddenDocs` resolved to **59 document numbers** (each reversal hides both the
cancelled document and the reversing document, including transfer `SND-R-*`
and correction `SND-C-*` pairs). `hiddenLegacyIds` resolved to **14 movement
ids**.

### Exclusion tally — 98 of 101 raw rows excluded

| Reason | Rows |
| --- | --- |
| document hidden (original/reversal pair) | 70 |
| named by a legacy cancellation id | 14 |
| row is itself a `Ləğv…` marker | 14 |

### Element-by-element equality

Independently expected surviving ids:

```
43036d8e-6bfe-421f-a23a-ed7efd77736c   TEST-IN-1
bd8369ee-f802-4cc5-b735-48b784b98b59   TEST-OUT-1
27dbfbe2-21a7-4267-a14d-07bba2026816   TEST-IN-2
```

Rendered rows resolved back to ids (via a `date|type|note` tuple **asserted
unique** across the expected set):

```
27dbfbe2-21a7-4267-a14d-07bba2026816   (03.09.2026 Alış)
bd8369ee-f802-4cc5-b735-48b784b98b59   (02.09.2026 Silinmə)
43036d8e-6bfe-421f-a23a-ed7efd77736c   (01.09.2026 Alış)
```

| Proof | Observed |
| --- | --- |
| missing visible rows | **0** |
| unexpected visible rows | **0** |
| count | 3 expected = 3 rendered |
| set equality | **YES** |
| footer from the same surviving set | independent `{count:3, in:11, out:3}` → rendered `3 qeyd · mədaxil 11.00 · məxaric 3.00 · mədaxil dəyəri 111.00 ₼` |

### M8-20 guard — partially modified documents are NOT whole-document cancellations

**11 documents** reach zero visible rows purely through row-level counters
while never being named by a document reversal, so they are correctly absent
from `hiddenDocs`. Worked example, `SND-12B8BCDD3A`:

| id | type | in/out | note | excluded by |
| --- | --- | --- | --- | --- |
| `424f6af4` | Satınalma | 2 / 0 | `… correction replacement · Əvəz edir: SN…` | named by legacy id |
| `7e47b797` | Satınalma | 0 / 2 | `Ləğv ID: 424f6af4-…` | is a `Ləğv` marker |
| `faa34c68` | Satınalma | 2 / 0 | `… correction replacement · Əvəz edir: SN…` | named by legacy id |
| `92b6d863` | Satınalma | 0 / 2 | `Ləğv ID: faa34c68-…` | is a `Ləğv` marker |

The document number never enters `hiddenDocs`; every row leaves individually.
This is exactly the distinction M8-20 exists to protect, and it is exercised
live by 11 documents.

**Material branches exercised:** whole-document reversal, ordinary/correction
(`SND-C-*`) pairs, transfer reverse (`SND-R-*`, `SND-LR-*`) pairs, legacy
single-row, legacy transfer pair, and row-level counters inside a surviving
document number. The row's exclusion branches are therefore materially
exercised, not incidentally.

---

## M8-05 — `Silinmə` valuation: **PARTIALLY LIVE VERIFIED** (one branch only)

Ledger contract: `Silinmə` price/amount via `movementValuation()`; others via
`m.pr → item.price`; `final == null` renders «—» and the unit price is 4-dp.

**Exactly one `Silinmə` row is visible.** RAW holds 7; the other 6 are
row-level cancelled and correctly outside `excludeCancelled()`.

| Field | Value |
| --- | --- |
| movement id | `bd8369ee-f802-4cc5-b735-48b784b98b59` (`TEST-OUT-1`) |
| `out_qty` / `price` | 3 / **0** |
| matching `writeoff_valuations` row | **absent** (confirmed by id lookup) |
| branch taken | **unvalued** — `pr <= 0` → `final = null` |
| rendered MƏBLƏĞ | `—` |
| rendered QIYMƏT | `—` |

Independent expectation and DOM agree.

### Why the stored-valuation branch is NOT promoted

`writeoff_valuations` returned **3 rows**, so the table is populated — but
**every one belongs to a row-level-cancelled movement**, hence none is
rendered:

| movement_id | final_amount | method | fate |
| --- | --- | --- | --- |
| `53b5bd5a-…` | 12.5 | `source` | excluded — named by legacy cancellation id |
| `8d8f82eb-…` | null | `unknown` | excluded — named by legacy cancellation id |
| `d14bac90-…` | 12.5 | `source` | excluded — named by legacy cancellation id |

**Branches deliberately left CODE VERIFIED — no live fixture, and none was
created:**

- stored-valuation hit (`final_amount` rendered from `writeoff_valuations`);
- the `unknown`/null-final stored branch;
- the legacy `qty × price` fallback (`pr > 0`, no stored row) — 4 raw `Silinmə`
  rows have `price > 0` but all are cancelled;
- `writeOffUnitPrice()`'s 4-dp quotient, which needs a non-null `final`.

M8-05 therefore stays **PARTIAL**. The ledger contract covers the stored source
and the fallback ordering, not only the em-dash outcome, so a promotion to full
LIVE VERIFIED would overclaim.

---

## Reconciliation

- RAW `movements`: **101**, freshly measured this run — unchanged.
- Non-GET requests, all read-only/session RPCs: `register_session`,
  `get_reference_values`, `get_user_directory`, `stock_layers_supported`,
  `list_my_sessions`, `end_session`. **No mutation RPC, no INSERT/UPDATE/DELETE,
  no fixture.**
- Production contact attempts: **0**.
- TEST device session ended through the real «Çıxış».
- Localhost remains TEST sandbox/read-only, HTTP 200,
  `VITE_ALLOW_LOCAL_WRITES=false`.
- Staged state empty; dirty working tree preserved at **213** entries.
- No I-10 row.

## Summary

| Row | Result |
| --- | --- |
| M8-03 | **LIVE VERIFIED** — independent set equality, all three cancellation families, M8-20 distinction proven |
| M8-05 | **PARTIALLY LIVE VERIFIED** — unvalued/em-dash branch only; stored + legacy-fallback branches remain CODE VERIFIED |
| M8-52 | **LIVE VERIFIED** — 16-column explicit list, both widened fields, single paging leg |

Phase 8 remains **NOT ACCEPTED**.
