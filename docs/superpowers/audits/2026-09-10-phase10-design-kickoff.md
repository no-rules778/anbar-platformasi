# Phase 10 design kickoff — legacy inventory

Date: 2026-09-10  
Verdict: **DESIGN RESEARCH STARTED; NO IMPLEMENTATION**

After Phase 9 acceptance, Codex read the post-Phase-9 roadmap and inspected the
legacy `anb` page, `rAnb()`, the `dead` report branch, loader classification and
shared `IX` index construction. The resulting draft proposal records only
contracts directly visible in those sources and names three decisions that
must be settled during design review.

No application code, SQL, TEST data or environment file was changed. No
Supabase or production contact occurred. The existing dirty tree remains
preserved and nothing was staged, committed, pushed or deployed.

A 36-row draft ledger and TEST-only draft implementation plan were then added
and checked for unique ids, file links, whitespace hygiene and empty staging.
All M10 rows remain `NOT STARTED`. Next work is independent design review and
owner decisions D-K1…D-K3. Implementation is not yet authorised.
