VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Localhost shares the PRODUCTION Supabase project. Destructive reference
# actions (delete / deactivate / activate) are blocked there unless this is
# exactly 'true'. Has no effect on a deployed build.
VITE_ALLOW_DESTRUCTIVE=false
